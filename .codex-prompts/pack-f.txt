你现在要执行 AgentDev Lite 项目的 Day 1 Pack F。

前置：Day 0 已完成（day0-complete tag 存在），server 能跑通用对话。根目录和 server/ 的 node_modules 都已安装好。

请打开并阅读这个 plan 文件：
D:\claude project\agentdev-lite\docs\superpowers\plans\2026-04-10-agentdev-lite.md

需要执行的任务，**严格按顺序**：
- Task 1.1（Server - services/docxGen.js）
- Task 1.2（Server - routes/word.js）
- Task 1.3（Server - routes/artifacts.js + open-file）

执行规则：
1. 工作目录固定为 D:\claude project\agentdev-lite\。Windows 路径里有空格，shell 命令里所有路径都加双引号。
2. Task 1.1 会要你在 server/package.json 加 `docx` 依赖，按 plan 里写的版本号装，**不要升级到 latest**。装完跑 `npm --prefix server install` 验证。注意：server/package.json 的 dependencies 里 Day 0 已经有 docx 了（"docx": "^8.5.0"），所以只需确认已装好即可，不需要再加。
3. docxGen.js 里的中文字体设置（宋体 / Times New Roman）、字号、对齐方式，全部照搬，**不要换字体名**。理由：宋体是 Word 默认中文字体名，换了之后 Word 打开会回退成英文字体。
4. routes/word.js 里调用 deepseek.chatJson 生成 sections 的 prompt 必须照搬 plan，**不要把"用 JSON 输出"那段去掉**。
5. routes/artifacts.js 里的 GET /api/open-file 端点必须有路径穿越保护（plan 里有 `path.resolve` + `startsWith` 检查），**不能省略**——这是安全要求。
6. server/index.js 在 Task 1.2/1.3 里会被改动两次（每次加一个新路由），按 plan 改。
7. 每个 Task 末尾的 git commit 必须执行。
8. 验证时本机 curl 对 localhost 走代理会返回 502，带 `--noproxy "*"`。

完成后必须验证（不要让 server 一直挂着，验完关掉）：
- `npm --prefix server install` 无错
- 后台启 `npm --prefix server run dev`，等 3 秒让它启好。
- 跑：
    curl --noproxy "*" -X POST http://localhost:8787/api/word -H "Content-Type: application/json" -d "{\"title\":\"测试报告\",\"topic\":\"人工智能简介\",\"sections\":3}"
  注意：这个接口需要真 DeepSeek API Key 才能成功。如果 data/config.json 里已有 apiKey 就直接跑；如果没有或返回 apiKey 相关错误，**停下来打印提示让用户先在浏览器里填 key，然后等用户确认后再试**。
  如果 key 可用，应返回 JSON，包含 filename、path 字段。
- 跑：
    curl --noproxy "*" http://localhost:8787/api/artifacts
  返回 JSON 数组，包含刚才的 .docx 条目。
- 跑（路径穿越测试）：
    curl --noproxy "*" "http://localhost:8787/api/open-file?path=../../../etc/passwd"
  必须返回 4xx 错误。如果返回 200 + 文件内容，**立刻停下报告**。
- kill 掉所有 node 进程。
- 跑 `git log --oneline`，应该比 day0-complete 多 3 个 commit。

最后停下来：
1. 贴出 curl 响应（/api/word 的返回 JSON、/api/artifacts 的返回、穿越测试结果）
2. 如果成功生成了 .docx，贴出文件路径和文件大小
3. 输出 `git status` 和 `git log --oneline -20`
4. 报告完成，**不要自动开 Pack G**。
